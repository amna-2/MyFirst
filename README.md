# MyFirst

A simple calculator built with Python and a cute pastel-colored UI using Tkinter.

## Run

```bash
python calculator.py
```

## Features

- Basic arithmetic (`+`, `-`, `*`, `/`, `%`)
- Decimal support
- Parentheses support
- Clear (`C`) and backspace (`⌫`) controls
- Friendly error message for invalid expressions
